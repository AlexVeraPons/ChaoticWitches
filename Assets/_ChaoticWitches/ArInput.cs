using UnityEngine;
using Vuforia;

public class ArInput : MonoBehaviour
{
    private AnchorInputListenerBehaviour _anchorInputListenerBehaviour;
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Space))
        {
        }
    }
}